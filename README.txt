The code files are numbered according to the figures in the main paper. The main code files are those named as `figxx.py`. 

There are a few additional code files and datasets that are included as dependencies.  

All external dependencies may be installed using pip or conda.

The code files may be run using the script `python figxx.py` in the console.

All generated figures will be saved in the main folder.

